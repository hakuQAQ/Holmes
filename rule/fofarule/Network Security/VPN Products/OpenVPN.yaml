info:
  company: OpenVPN Inc.
  product: OpenVPN
  softhard: '2'
matches: |
  header.icontains("realm=\"openvpn")
  header.icontains("openvpn") && banner.icontains("openvpn") && banner.icontains("server:")
