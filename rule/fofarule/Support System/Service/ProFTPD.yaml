info:
  company: Other
  product: ProFTPD
  softhard: '2'
matches: |
  protocol.icontains("ftp")
  banner.icontains("proftpd") && header.icontains("realm=\"proftpd")
  banner.icontains("realm=\"proftpd")
