info:
  company: Other
  product: Broadband-Router
  softhard: '1'
matches: |
  header.icontains("realm=\"broadband router")
  banner.icontains("internet broadband router")
  title.icontains("broadband router")
