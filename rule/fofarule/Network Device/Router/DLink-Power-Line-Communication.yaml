info:
  company: DLink
  product: DLink-Power-Line-Communication
  softhard: '1'
matches: |
  header.icontains("dhp-") && header.icontains("server") && banner.icontains("dhp-") && banner.icontains("server")
