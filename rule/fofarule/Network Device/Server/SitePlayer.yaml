info:
  company: NetMedia Inc.
  product: SitePlayer
  softhard: '1'
matches: |
  header.icontains("siteplayer telnet")
  header.icontains("realm=\"siteplayer telnet configuration")
  banner.icontains("realm=\"siteplayer telnet configuration")
