info:
  company: Apache Software Foundation.
  product: Apache-ActiveMQ
  softhard: '2'
matches: |
  header.icontains("jetty") && title.icontains("apache activemq")
  banner.icontains("server:activemq")
  header.icontains("realm=\"activemqrealm") && banner.icontains("magic:activemq")
  banner.icontains("realm=\"activemqrealm")
