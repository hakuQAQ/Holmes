info:
  company: dptech
  product: Dptech-UMC
  softhard: '2'
matches: |
  header.icontains("jsessionid") && header.icontains("umc") && header.icontains("dptech umc server")
  body.icontains("onsubmit=\"return sys_submit(this)")
