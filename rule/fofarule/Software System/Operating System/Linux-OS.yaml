info:
  company: Other
  product: Linux-OS
  softhard: '2'
matches: |
  header.icontains("linux")
  header.icontains("unix") && title.icontains("28ze") && banner.icontains("linux") && banner.icontains("unix")
