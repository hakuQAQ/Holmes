info:
  company: Apple Inc.
  product: MacOS
  softhard: '2'
matches: |
  banner.icontains("macos")
  header.icontains("mac") && banner.icontains("macintosh os x")
  protocol.icontains("snmp") && banner.icontains("darwin kernel version")
